import React from "react";
import { ScrollView } from "react-native-gesture-handler";
import { Text } from "react-native";
const BlackTea = () => {
    return(
        <ScrollView>
            <Text>Черный кофе</Text>
        </ScrollView>
    )
}

export default BlackTea;