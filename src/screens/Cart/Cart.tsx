import React from 'react'
import { ScrollView, View } from 'react-native';
import Header from '../../components/Header';

const Cart = () => {
    return(
        <View>
            <Header name='Корзина'/>
            <ScrollView>

            </ScrollView>
        </View>
    )
}

export default Cart;